services:
  nsl-spbstu-news-bot:
    build:
      context: .
      dockerfile: Dockerfile
    command: main.py
    restart: always
    volumes:
      - data:/home/nsl-spbstu-news-bot/data

volumes:
  data:
    
